# UMI data-cleaning reports

- [umi400h / v0_study](umi400h/v0_study/report.html) — threshold study. Tabs: **Report** (before/after
  + funnel + dedup highlights) · **VIO Filter** (episode-level drift review) · **Yaw Review**
  (180° threshold calibration)
- [umi400h / v1a_dedup_probe](umi400h/v1a_dedup_probe/index.html) — strategy-a dedup probe
- [umi400h / v6_threshold_signoff](umi400h/v6_threshold_signoff/calm_blind.html) — calm_floor 盲判 40 样本(已判) + replay τ=0.3 边界盲看 20 段([replay_tau](umi400h/v6_threshold_signoff/replay_tau.html))
  (tau_vis .988, tau_act .065, motion off)
- [umi400h / v5_replay_viovisual_study](umi400h/v5_replay_viovisual_study/index.html) — replay +
  vio_visual threshold-decision study. Tabs: **Replay** (IK re-playability, s2c) · **VIO-visual**
  (frame-vs-VIO drift, s2d) · **Pose quality**. Full library: 200,551 segments (s2c) + 10,878 episodes
  (s2d). Pipeline verdict version `v3_replay_viovisual_study`.

## egofinger1800h

- [egofinger1800h / v1_ported_20260717](egofinger1800h/v1_ported_20260717/report.html) — 1800h
  ego_finger full-corpus verdict review: keep 211,877 / 673,738 segments (31.4%); kills dedup 52.3%
  · replay τ=0.3 14.6% · vio_visual 5 m 1.6%. Companion page:
  [action_threshold.html](egofinger1800h/v1_ported_20260717/action_threshold.html) (dedup ε study,
  100-pair blind review: false_merge=0 at ε=0.1). Static pipeline-S8 artifact (like
  `v5`/`v6` above, not rebuilt by `sitegen/`); pipeline config `configs/egofinger1800h.toml`.

## Rebuilding

Every page is generated by `sitegen/` in this repo — no dependency on the cleaning pipeline:

```bash
python3 -m sitegen.build          # rebuild every page of every run, in place
python3 -m sitegen.build --check  # verify the committed HTML is what the generator produces
```

`v0_study` is rebuilt from the snapshots in `data/`. `v1a_dedup_probe` is a finished probe whose
upstream numbers are gone: it is re-rendered from its own published pages, so it still tracks the
shared nav and design system, but its content can no longer be recomputed. See `CLAUDE.md`.
