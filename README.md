# UMI data-cleaning reports

- [umi400h / v0_study](umi400h/v0_study/index.html) — threshold study (placeholder verdicts)
- [umi400h / v1a_dedup_probe](umi400h/v1a_dedup_probe/index.html) — strategy-a dedup probe (tau_vis .988, tau_act .065, motion off)

## Maintenance notes

- `umi400h/v0_study/vio_filter.html` (VIO Filter tab) is generated by
  `cosmos-framework:experiments/umi400h/bench/build_vio_filter_page.py`, NOT by the
  cleaning pipeline's `build_site()`. Regenerating the site rewrites the other pages
  and drops the "VIO Filter" nav tab from them (the page itself survives) — rerun the
  builder with `--patch-nav` after any regen to restore the tab.
